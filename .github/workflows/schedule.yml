name: Run

on:
  schedule:
    - cron: "0 2 * * *"
  workflow_dispatch:

jobs:
  borrow:
    runs-on: ubuntu-latest
    env:
      UV_CACHE_DIR: /tmp/.uv-cache

    steps:
      - uses: actions/checkout@v6

      - name: Restore uv cache
        uses: actions/cache@v5
        with:
          path: /tmp/.uv-cache
          key: uv-${{ runner.os }}-${{ hashFiles('uv.lock') }}
          restore-keys: |
            uv-${{ runner.os }}-${{ hashFiles('uv.lock') }}
            uv-${{ runner.os }}

      - name: "Set up Python"
        uses: actions/setup-python@v6
        with:
          python-version-file: ".python-version"

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Install the project
        run: uv sync --locked --all-extras --dev

      - name: Restore last_borrowed.pickle
        uses: actions/cache@v5
        with:
          path: last_borrowed.pickle
          key: last-borrowed-${{ github.ref }}
          restore-keys: |
            last-borrowed-

      - name: Run Borrow
        run: |
          set -o pipefail
          uv run main.py foxit adobe zoom 2>&1 | tee log.log
        env:
          LOGIN_EMAIL: ${{ secrets.LOGIN_EMAIL }}
          LOGIN_PASSWORD: ${{ secrets.LOGIN_PASSWORD }}

      - name: Logging
        if: always()
        run: |
          {
            echo "## Log"
            echo ""
            echo '```'
            cat log.log || echo "(log.log not found)"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Minimize uv cache
        run: uv cache prune --ci
